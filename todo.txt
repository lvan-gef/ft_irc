check if we have func for setUsername and what the command for it is
user already in channel and when invalid channel msg is not correctly formated
When part the channel we get 2 messages for removing and add operator for the client that leaves
